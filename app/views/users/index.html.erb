<h1>User search</h1>
<%= form_with url: users_path, method: :get, local: true do |f| %>
  <%= f.text_field :skill %>
<% end %>
<table>
  <thead>
    <tr>
      <th>Language</th>
    </tr>
  </thead>
  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.skill %></td>
      </tr>
    <% end %>
  </tbody>
</table>